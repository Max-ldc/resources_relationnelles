import CreateUser from "../../service/CreateUser";

const CreerCompte = () => {
    return (
      <div>
        <h2>Créer un compte !?</h2>
        <CreateUser/>
      </div>
    );
  };
  
  
  
export default CreerCompte;